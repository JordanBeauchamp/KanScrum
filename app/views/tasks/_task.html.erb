<div class="well well-<%= task.task_type.downcase %>", id=<%= task.id %>>
  <p>
  	<b>
  		<%= task.task_type %>
  	</b>
  </p>
	<p>
		<b>
			Description: 
		</b>
		<%= task.description%>
	</p>
	<strong>
		<p id='p<%= task.id %>'>
			<% if task.user_name %>
				<%= 'Assigned to: ' << task.user_name %>
			<% else %>
				<%= 'Unassigned' %>
			<% end %>
		</p>
	</strong>
	<% if task.user_name %>
		<%= image_tag task.user.image, :class => 'avatar-image', :id => "image" + task.id.to_s %>
	<% end %>
	<button id=<%= "assign" << task.id.to_s %> class="btn btn-info">jump on it!</button>
	<div class="circle">
		<div class="circle-text">
			<b><%= task.hours %></b>
		</div>
	</div>
</div>